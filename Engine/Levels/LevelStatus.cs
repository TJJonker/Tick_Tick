namespace Engine
{
    /// <summary>
    ///  An enum containing the three possible level statuses
    /// </summary>
   public enum LevelStatus
    {
        Locked,
        Unlocked,
        Solved
    }
}
